import React from 'react';
import ReactDOM from 'react-dom';

import App from './App'; //mount the app to root 

ReactDOM.render(<App/>,document.getElementById('root')); // root is the root node of react

